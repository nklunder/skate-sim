# 📈 CHANGELOG_LEDGER.md - Skate Sim V2 Session & Progress History

This file maintains the running record of completed tasks, feature deployments, calibration refinements, and session logs for **Skate Sim V2**. AI agents should log significant milestone completions here at the end of sessions rather than in `AGENTS.md`.

---

## Completed Session Ledger
- [x] **Phase 0 & 1:** Foundation kinematics, stance detection, face-button pushing (Standard vs. Mongo), and manual balance tolerance zones.
- [x] **Phase 2 & 3:** 3-layer pop jump physics, shoe hover catching, Kickflip/Heelflip flick classification, and Shove-it rotation alignment.
- [x] **Visual Polish & Debugger:** Procedural parabolic concave deck mesh, anatomical standing ankle pegs with joystick tilt, and rounded sneaker profiles.
- [x] **Trigger Physics & Precision Landings:** Calibrated body rotation speed (`554 deg/s` for flat-ground 360s), audio-log trigger sensitivity curve (exponent `2.2`), 80% pre-wind turning suppression during pop loading, fluid angular momentum smoothing, and $\pm 45^\circ$ sideways landing bail tolerances.
- [x] **Advanced Shove-its & Gameplay Pace:** Integrated dynamic arc span tracking for Quarter-Turn ($180^\circ$) vs. Half-Turn ($360^\circ$) Shove-its, automatic Tre Flip / Laser Flip recognition, and global 80% simulation speed calibration.
- [x] **Switch Stance & Manual Inversion Resolution:** Solved stationary switch stance reversal by testing against static forward vectors at zero velocity, and applied Euler rotation angle inversion for all Switch/Fakie manuals, pops, and flips.
- [x] **Top Speed Calibration Update:** Adjusted cruising max speed (`max_push_speed`) from `5.6 m/s` to `7.0 m/s` ($\approx 15.7\text{ mph}$) and exported motion physics parameters in `SkaterController.gd`.
- [x] **Shove-it Scoop Sensitivity Refinement:** Lowered the 360 Shove-it / Tre Flip trigger threshold from `110°` down to `80°` for more effortless quarter-turn sweeps, and exported both `shuv_180_threshold_deg` and `shuv_360_threshold_deg` in `FootInputState.gd`.
- [x] **Skateboard 3D Model Integration:** Replaced the procedural placeholder skateboard with the imported `res://skateboard.blend` model in `SkateDeckMesh.gd` and updated `SkaterRig.tscn` to use `Node3D` for `BoardMesh`. Implemented calibrated scaling, centering offset, LOD deduplication, and automated material surfacing.
- [x] **Raycast Surface Collision (Stage 1 of 2):** The skater now rides real geometry instead of a hardcoded `y = 0.25` plane — lands on curb tops, follows the ramp, drops off ledges, and stops against vertical faces. Added `SurfaceProbe.gd` (direct space queries, including spline-free edge detection ready for Stage 2 grinding) and a dedicated `SurfaceAlign` node so surface tilt never shares a rotation channel with a trick system. Verified headlessly: probe geometry (ground/curb/ramp normals + curb edge direction), settling at `surface + ride_height` on all three, ramp tilt matching, ledge drop-off, wall blocking, and all six trick-naming suites still passing as direct evidence of no interference.
- [x] **Data-Driven Trick Naming:** Replaced the string-concatenation naming tree with a measured `TrickSignature` plus an editable rule table (`scripts/tricks/`). Names now resolve at touchdown rather than at pop, so body rotation counts — making bigflip, frontside flip and body varial expressible. Severed the display-name→physics coupling that let a rename change board spin speed. Follow-up fixes corrected two silent mirror-image bugs: the shuv sign (the scooping foot swaps ends between stances) and the body sign (body rotation is measured in world space and needs flipping when riding reversed).
- [x] **Low-Poly LOD Default:** Flipped `use_low_poly` to `true`, dropping drawn geometry from `646,208` to `8,328` tris (`77.6x`) with no visible change at chase or underside distance. The two-tone deck shader carries over to `board_low` unmodified.
- [x] **Foot Seating & Ankle Peg Direction Fix:** Pulled the placeholder foot meshes out to `X = -0.025m` so the heel edge clears the heel rail and more of the toe sits on the deck (overhang balanced to `0.043m` / `0.042m`), and moved rest-pose capture into `SkaterController._ready()` so `SkaterRig.tscn` is the single source of truth. Fixed the ankle pegs reading reversed in Switch stance by resolving the stick vector through `board_pivot.rotation.y` instead of driving pivot-local degrees directly.
- [x] **Two-Tone Deck for Trick Readability:** Replaced the flat griptape-coloured deck with `deck_two_tone.gdshader`. Griptape black stays on the standing side while the underside runs high-visibility orange (`Color(0.95, 0.30, 0.02)`) so board rotation reads clearly mid-air. Includes a procedural two-octave griptape grain driven by normal perturbation and a `bottom_emission` floor of `0.30`.
- [x] **Foot Mesh Refinement & Camera Pull-In:** Sized foot meshes to US 11 M dimensions (`28 cm` long by `9.8 cm` wide, `3.8 cm` tall), adjusted heel peg articulation offsets (`-0.075m`), and pulled the chase camera closer (`Y = 1.25m, Z = 2.1m`) for improved deck and foot visibility.
- [x] **Stance Flip Direction & Shove-it Decoupling Fix:** Decoupled trick spin/flip rotation from $180^\circ$ deck yaw reversals after Shove-its in `SkaterController.gd`, inverted roll directions to correct backward Nollie and Switch flips, and corrected backwards HUD trick name labeling for Fakie kickflips/heelflips in `FootInputState.gd`.
- [x] **Varial & Shove-it Spin Direction Decoupling Fix:** Removed board orientation coupling (`deck_orientation_comp`) from `spin_sign` in `SkaterController.gd`. Unlike flip roll (Z-axis), horizontal yaw spins (Y-axis) remain independent of the wooden board's $180^\circ$ orientation, ensuring consistent clockwise/counterclockwise scoops relative to the skater when leaping off the nose versus tail.
- [x] **True Angular Sweep & Shove-it Buffer Refinement:** Upgraded Shove-it scoop detection from static stick coordinates to signed rotational sweep direction (`angle_difference`) in `FootInputState.gd` and `SkaterController.gd`. Set generous quarter-turn ($45^\circ \to 134^\circ$) and half-turn ($\ge 135^\circ$) buffer windows so starting position on the thumbstick circle no longer affects rotation accuracy.
- [x] **Frame-Delta Sweep Accumulator & Effortless 360 Shuv Calibration:** Solved occasional inverted 360 Shove-it spin animations by replacing static `angle_difference(start, current)` with frame-to-frame delta accumulation (`accumulated_scoop_deg += frame_delta`), completely preventing half-circle wrap-around bugs when sweeps exceed $180^\circ$. Lowered the 360 Shove-it trigger threshold from $135^\circ$ to $95^\circ$ and 180 Shove-it threshold to $40^\circ$ for smooth execution.
- [x] **Universal Touchdown Foot Settlement & Grounded Posture Maintenance:** Resolved a bug where shoes hovered above the board after landing a trick until a new input occurred. Implemented instant foot coordinate reset right at touchdown (`_evaluate_touchdown_landing`) and persistent standing posture enforcement during idle rolling on pavement (`if active_push_foot == ""`) in `SkaterController.gd`.
- [x] **Debug HUD Styling Refinements:** Upgraded the root `Panel` to a dynamic `PanelContainer` with `16px` content margins in `DebugHUD.tscn`, ensuring multi-line signature readouts automatically expand the background panel without squishing top or bottom margins. Converted leading and trailing foot readouts to `RichTextLabel` with BBCode coloring (`#3399e6` for Left Foot, `#e64d4d` for Right Foot) to match actual shoe box albedo colors.
- [x] **Nollie & Fakie Flip Classification Alignment:** Solved inverted trick display terminology where Nollie BS Flip displayed as "Nollie BS Heelflip" and vice versa. Updated `FootInputState.gd` to mirror both `NOLLIE` and `FAKIE_OLLIE` flip labels natively, and removed the artificial `-1.0` roll inversion in `SkaterController.gd` so physical deck rotations remain mathematically accurate without contradictory HUD naming.
- [x] **Testing Environment Perimeter Walls & Banked Platform:** Added four 4.0m-tall perimeter boundaries around the $60\text{m} \times 60\text{m}$ ground plane in `TestWorld.tscn` using a tinted semi-opaque refractive glass material (`Color(0.45, 0.65, 0.8, 0.45)`). Introduced a raised $6\text{m} \times 8\text{m}$ concrete skate platform (`Y = 0.8\text{m}`) at `X = -10, Z = -18` featuring a smooth $12.9^\circ$ access ramp on its south end for manuals and gap launches.
- [x] **Dynamic Push Stroke Stance Alignment:** Resolved an issue where both feet touched down on the wrong side of the deck during Goofy stance pushes. Replaced hardcoded left/right shoe reach directions in `_animate_foot_push_stroke` with dynamic alignment checking `input_state.leading_foot`: across all stances, Standard Pushes (using trailing foot) consistently step off onto the front toe side ($+X$), while Mongo Pushes (using leading foot) reach out to the rear heel side ($-X$).
- [x] **Spawn Position Clearance Adjustment:** Relocated the default starting transform of `SkaterRig` in `TestWorld.tscn` from `(0, 0.078, 0)` to `(0, 0.078, 6)` along the Z-axis, moving it out of overlap with `Curb1` and giving an open 6-meter approach run toward central obstacles.
- [x] **Standalone GLB Asset Migration:** Exported `res://skateboard.glb` directly from `skateboard.blend` via Blender CLI and updated `SkateDeckMesh.gd` to preload the native `.glb` file.
- [x] **Velocity Vector, Wheel Grip & Slope Gravity:** Replaced `velocity = -global_transform.basis.z * current_speed` with an authoritative world-space velocity vector. Touchdown no longer snaps board yaw to a perfect $0/180^\circ$; residuals are transferred to the rig yaw. Wash-out bails are now judged on lateral momentum against `max_landing_slide` (`2.5 m/s`). Rolling friction opposes travel via `move_toward`, enabling holding on gentle gradients, rolling down steep ones, and reversing when stalled.
- [x] **Physics-Driven Deck Catch & Axis Synchronisation:** Landing is judged on orientation against `catch_cone_deg = min(atan(grip_friction), rail-strike angle)`. A late catch costs `cos(catch_error)` of speed, and `_credit_achieved_rotation()` rewrites the signature to the rotation the deck actually completed. Rotation is imparted once at pop as constant angular velocity, with both axes scaled onto one shared trick duration in `_impart_deck_rotation()`. `_advance_flip_settle()` carries a late-caught deck onto resting orientation at existing velocity without teleporting.
- [x] **TestWorld Collision Shape Alignment:** Updated `BoxShape3D_curb` size and `Curb1/CollisionShape3D` transform in `TestWorld.tscn` to precisely match their corresponding mesh dimensions (`Vector3(7.9971313, 0.3, 6.585791)`) and transform offsets (`(-0.06680298, 0, 1.6421511)`).
- [x] **Debug HUD Layout & Controller Button Telemetry:** Pinned the telemetry overlay to the left screen edge spanning full height (`anchors_preset = 9`, `anchor_bottom = 1.0`, `offset_right = 460.0`), removed top/left/bottom borders in `DebugHUD.tscn`, and integrated real-time gamepad button polling across all 64 joypad button indices in `FootInputState.gd` and `DebugHUD.gd`.
- [x] **Controller-Navigable Settings Menu & Debug HUD Toggle:** Built `SettingsMenu.tscn` and `SettingsMenu.gd` as a dedicated settings overlay triggered by Button 6 (`JOY_BUTTON_START`) or Button 1 (`JOY_BUTTON_B` to close). Opens with gameplay paused, runs on layer 10 over a dimmed navy backdrop, and automatically focuses the "Show Debug HUD" checkbox for D-pad/stick navigation.
- [x] **Debug Overlay Camera Centering Evaluation (via `/grill-me`):** Evaluated dynamic horizontal camera offsetting (`Camera3D.h_offset`) to laterally displace the camera when UI overlays open. Reverted and discarded: the camera is intentionally kept dead-center on screen at `h_offset = 0.0` regardless of UI state.
- [x] **Gravity Rollback Controls & Zero-Speed Direction Preservation (via `/grill-me`):** Updated `_animate_ankle_pegs` to map joystick vectors onto `BoardPivot` axes via camera-relative angle (`angle_difference(camera_pivot.rotation.y, board_pivot.rotation.y)`), ensuring ankle pegs never appear backwards after the camera swings $180^\circ$ around `SkaterRoot`. Implemented a persistent rolling direction tracker (`_travel_axis_sign`) updated above `camera_travel_min_speed`, ensuring stop-to-start pushes and switch stance tracking preserve true rolling direction after slope rollbacks.
- [x] **Kick/Push Button Remapping:** Remapped left foot and right foot push/kick inputs in `FootInputState.gd` from Joypad Buttons 2 and 0 (`JOY_BUTTON_X` / Square and `JOY_BUTTON_A` / Cross) to Joypad Buttons 3 and 1 (`JOY_BUTTON_Y` / Triangle and `JOY_BUTTON_B` / Circle), respectively.
- [x] **Gamepad Debug HUD Toggling in Settings Menu:** Added explicit handling for Joypad Button 0 (`JOY_BUTTON_A` / Cross) in `SettingsMenu._input` to toggle the currently focused `BaseButton` (or fallback to `hud_checkbox` if focus was lost).
- [x] **Variable Flip Tilt & Asymmetric Ergonomic Flick Windows (Boned & Rocketed Tricks):** Implemented dynamic mid-air skateboard pitch based on diagonal thumbstick flick inputs (`flick_tilt_deg` in `TrickSignature.gd`). Exported parameters in `FootInputState.gd`: `max_flick_up_angle_deg = 60.0`, `max_flick_down_angle_deg = 45.0`, `max_boned_tilt_deg = -18.0`, `max_rocketed_tilt_deg = 18.0`. During `_apply_airborne_board_pitch`, active flip rotations target this measured tilt—causing nose-pocket flicks to pitch down ("boned") and side/down flicks to pitch up ("rocketed").
- [x] **Pop & Flick Stick Diagnostic Zone Circles in Debug HUD:** Built `res://scripts/ui/StickZoneDiagram.gd` as a real-time component rendering interactive circular instrument gauges in `DebugHUD.tscn`. Displays Pop & Scoop Zone Circle and Flick & Flip Zone Circle with glowing thumbstick indicator dots and anatomical labels. Compacted panel layout by styling `"Last Trick"` header and reducing container height from 160px to 110px so telemetry HUD fits cleanly on screen.
- [x] **Session-Style Pop Impulse Control (Directional Ollie / Nollie Jumping):** Implemented off-center pop stick directional control in `FootInputState.gd` and `SkaterController.gd` (via `/grill-me` alignment). When loading an Ollie (Tail) or Nollie (Nose), horizontal stick deflection (`stick.x`) outside a small deadzone (`|x| < 0.15`) scales smoothly up to `0.70` deflection into a lateral directional ratio (`pop_lateral_impulse_ratio`). Upon pop release, `SkaterController.gd` injects lateral world velocity (`max_lateral_pop_impulse = 1.5 m/s` scaled by `_travel_axis_sign`) perpendicular to rolling direction alongside a subtle cosmetic yaw twist (`lateral_pop_yaw_deg = 5.0°`) that naturally aligns upon landing via $\pm 45^\circ$ touchdown tolerances. Automatically disables directional pop impulse ($= 0.0$) when an arc sweep ($\ge 40^\circ$) is detected for Pop Shove-its or 360 Flips.
- [x] **Structural Refactor — Frame Pipeline, Extracted Presentation Nodes & Isolated Device Polling (behaviour-preserving):** Split the monolithic `SkaterController.gd` (1138 lines) along its real seams, with zero gameplay change — both regression suites reproduce every printed figure exactly (12/12 + 12/12). Created `res://scripts/player/ChaseCamera.gd` (on `CameraPivot`, owning all framing tunables), `res://scripts/player/FootRig.gd` (under `BoardPivot`, owning shoe boxes / ankle pegs / push strokes / flip hover), `res://scripts/input/StickPoller.gd` (the only code that knows a gamepad exists, returning a `Sample`), and `res://scripts/ui/JoyButtonNames.gd` (debug button labels, moved off the physics tick). `_physics_process` is now an explicitly ordered pipeline of named steps (`_update_grounded_state`, `_apply_push_inputs`, `_execute_pop`, `_integrate_flight`, `_integrate_position`, …); the extracted nodes are advanced by explicit calls rather than their own `_physics_process` so ordering stays visible in code instead of depending on node order in `SkaterRig.tscn`. Replaced the last stringly-typed **logic** with enums (`active_flip: TrickSignature.Flip`, `active_push_foot: FootInputState.Foot` + `is_pushing`); display strings remain strings. Renamed `camera_travel_min_speed` → `travel_min_speed` on `SkaterController`, since both the camera heading and `_travel_axis_sign` consume it and must not disagree.
- [x] **Regression Caught & Test Hardened — Camera Side Clobbered by Device Polling:** Extracting the poller initially made it publish a sticky `camera_side` every frame, overwriting any value set from outside the device layer. The `bank reversal, left view` case silently reproduced the right-view result (`camVsTravel -2.5` instead of `+2.5`) and still **passed**, because that assertion only checked magnitude. Fixed by having `Sample.camera_side_select` report `0` when the d-pad is idle, leaving the sticky side owned by `FootInputState`. Hardened `tests/ground_physics.gd` by opting both reversal cases into the pre-existing `expect_swing_sign` assertion (`-1` right view, `+1` left view) so the sign can no longer drift unnoticed.
- [x] **Stationary Kickturns with Carve Latching & Manual Sovereignty:** Implemented stationary axle pivot turning at dead stops and slow speeds in `SkaterController.gd` (via `/grill-me` design alignment). When grounded rolling speed is below `@export var kickturn_max_speed: float = 0.5` m/s and a shoulder trigger is squeezed (`abs(input_state.lean) > 0.05`), steering shifts from center-origin carving to true trailing axle anchoring: `global_position` shifts dynamically by the delta of `to_global(axle_local_pos)` before and after rotation so rear wheel tires stay glued to pavement while the nose swings in an arc. Integrated **Carve Action Latching (`_is_carve_latched`)** so turns initiated above `0.5 m/s` smoothly remain in carving mode down to near-stall (`< 0.05 m/s`) without sudden mid-carve kickturn interruptions. Implemented **Manual Protection (`is_manualing`)** so steering while actively balancing a nose manual or tail manual preserves user pitch authority without overriding balance angles.
- [x] **Pressure-Scaled Manual Flick-Outs & Low-Pop Ledge Drops:** Expanded pop loading and flick recognition thresholds in `FootInputState.gd` so any active manual balance ($0.20 \to 0.90$) allows initiating flip tricks and scoops without forcing a heavy pop lock ($\ge 0.70$). Implemented dynamic analog pressure scaling via `pop_impulse_scale` in `_execute_pop()` in `SkaterController.gd`: gentle stick holds ($0.20 \to 0.55$) impart a low-pop or un-popped impulse ($0\% \to 25\%$ jump impulse) for authentic street drops off ledges and curbs, while heavy compression ($\ge 0.70$) ramps to full manual pop height ($75\%$). Defined `is_preparing_pop()` helper method to guarantee grounded carving speed is never damped during gentle manual balance holds.
- [x] **Radial Manual Continuity, Scoop Latching & Ollie Compression Sovereignty (Resolved Active Bug):** Solved accidental manual drop-outs when scooping Pop Shove-its, Varial Flips, or heavy-popped Kickflips out of manuals, while simultaneously resolving our active bug for over-pitched landing bails when holding thumbsticks past $0.90$ through touchdown. Implemented a 2-stage balance law in `SkaterController.gd` (`was_manualing` check): entering a new manual from 4-wheel riding requires precision balance ($0.20 < y \le 0.90$) so full tail compression ($\ge 0.90$) cleanly prepares flatground Ollies and tail-pocket lateral positioning without raising the nose. Once an active manual is established, balance tracking smoothly shifts to polar vector magnitude (`length() >= 0.20`), holding truck pivot tilt stable during full circular Shove-it scoops and deep pop deflections without dropping out or clashing with touchdown tolerances.
- [x] **Asset Reorganization & 3D Shoe Model Integration:** Organized project 3D models into idiomatic directories (`res://assets/models/decks/` and `res://assets/models/shoes/`), updating `SkateDeckMesh.gd` to load `skateboard.glb` from its new home. Created `ShoeMesh.gd` (`class_name ShoeMesh`, registered in engine cache) and swapped out the placeholder cube meshes on `LeftFoot` and `RightFoot` in `SkaterRig.tscn` for dynamic instances of `shoes_dc_tonik_right.glb`. Implemented automatic scale mirroring for opposite shoe matching (`is_left_shoe = true/false`) and preserved diagnostic telemetry colorization via dynamic albedo texture tinting (sapphire blue tint for Left Foot / Triangle push, ruby red tint for Right Foot / Circle push). Verified 24/24 regression tests pass with exact numerical fidelity.
- [x] **Landing Skid Fixed — Grip Pacing Is Now a Shrinking Budget:** Solved a reported skid where holding a trigger through a touchdown slid the board sideways for as long as it was held (measured at **89.8° off the rolling axis**, grip capped 127 of 150 frames). `_realigning` was a boolean latch that weakened grip after a landing and cleared once live lateral speed fell under `0.02 m/s` — but steering generates lateral at ~21 m/s² against the ~7 m/s² capped grip, so the clear condition never arrived and the rider drove a positive feedback loop directly. Replaced with `_landing_residual`, a budget armed once at touchdown with the sideways speed actually arrived with, draining at the paced rate, with full grip removing everything above it. The landing swing is bit-identical to before (`dir 1.00`, `cam 1.04` on the 185°/175° cases) while a held trigger now settles to the normal 2.9° carve figure within 17 frames. Added three regression cases covering both lean signs against both residual directions — the hole that let this mechanism be **wrongly dismissed** earlier, when the disproving test carved after a straight landing and never reached the state it was meant to test. 35/35 across three suites.
- [x] **Two-Stage Balance Law Deduplicated:** `_apply_grounded_board_pitch()` and `_evaluate_touchdown_landing()` each wrote out the same non-obvious compound predicate for "is the rider balancing this end down?" — the fix for BUG_ARCHIVE #5 — leaving them one edit away from disagreeing about what a manual is. Both stages now live on `TrickState` as `enters_tail_balance()` / `enters_nose_balance()` (stage 1, from four wheels: mid-zone precision **and** no active scoop) and `holds_tail_balance()` / `holds_nose_balance()` (stage 2, already balancing: polar magnitude during a pop load). Exported `manual_entry_max`, `scoop_idle_deg` and `manual_catch_min_pitch_deg`, the last literals in that path. 32/32 unchanged.
- [x] **Input Split — `RiderInput` / `TrickState` (behaviour-preserving):** Split the 380-line `FootInputState`, which was simultaneously an input reader, a gesture/trick state machine, and a scratchpad `SkaterController` wrote back into from six call sites. `RiderInput.gd` now owns stick vectors, magnitudes, latched buttons and stance facts and **knows nothing about tricks**; `TrickState.gd` owns pop loading, scoop arc measurement, flick classification and the `TrickSignature`. `TrickState` is a **child** of `RiderInput` rather than a sibling, because Godot runs `_physics_process` in tree order — that arrangement is what guarantees the gesture recogniser reads a fully-updated stick. Exported nine deflection thresholds that were literals (`manual_zone_min`, `pop_load_threshold`, `low_pop_knee`, `low_pop_max_ratio`, `flick_min_deflection`, `flick_release_band`, `scoop_min_deflection`, `lateral_pop_deadzone`, `lateral_pop_full`), satisfying rule #7 of the foot-animation spec. Fixed a latent coupling in the process: the lateral-pop lockout hardcoded `40.0`, the same value as `shuv_180_threshold_deg`, so retuning the shuv threshold would have silently desynchronised them — it now keys off the export itself. Extracted `_begin_load()` and `_release_pop()` from the two near-duplicate ollie/nollie branches. 32/32 across three suites reproduce every printed figure exactly.
- [x] **Rig Frame Conversion Layer & Lean Authority (behaviour-preserving):** Consolidated **seven independently-derived orientation sign compensations** into one documented conversion layer on `SkaterController`, following the pattern `TrickSignature.gd` already used for the rotation-*naming* frames — whose conversions are the only sign logic in this project that has never regressed. The rig's three frames (`SkaterRoot` → `BoardPivot` → `BoardMesh`) and every conversion between them are now stated once, in one block. New API: `leading_axle_z()` / `trailing_axle_z()`, `stance_sign()`, `rider_pitch_deg()`, `pivot_reversed()`, `pivot_z_to_rig()`, `deck_reversed()`, `flip_roll_sign()`. Retired four separate `left_is_front` branches (in `_execute_pop`, `_apply_grounded_board_pitch`, `_apply_airborne_board_pitch` and `_evaluate_touchdown_landing`), the ad-hoc `deck_flip` from the kickturn fix, and two duplicate `cos(board_mesh.y)` derivations. Pitch logic is now written **rider-relative** throughout and converted once at the single line that assigns `board_pivot.rotation_degrees.x`. Separately, folded the accumulating `turn_mult` special cases into `_lean_authority()`, a single named home for how much weight is available to lean the deck with — pop loading and pushing register as contributors, and grinds and manuals will too. Exported six values that were literals in tick equations (`max_pitch_deg`, `pop_pitch_deg`, `pitch_stick_deadzone`, `airborne_pitch_follow`, `grounded_pitch_follow`, `pop_load_turn_damping`). All 32 cases across three suites reproduce every printed figure exactly.
- [x] **Switch-Stance Kickturn Anchor Fix & Push Steering Damping:** Fixed a frame mismatch that made every kickturn in switch or fakie pivot on the **airborne** truck. `_apply_steering()` chose the anchor axle from `leading_foot` (resolved in `BoardPivot`'s basis, which carries the 0/180 switch flip) but applied it via `SkaterRoot`'s `to_global()` (which does not), inverting the Z sign whenever the pivot sat at 180 - i.e. all of switch/fakie, and after any landed body-180. Since `_apply_grounded_board_pitch()` lifts the leading trucks for a kickturn, the anchor was exactly the truck in the air: the grounded axle swung a measured **0.940 m** arc around it. `_apply_manual_pivot()` had always been correct, so the two silently disagreed. Also added **push steering damping** (`push_turn_damping = 0.15`, `push_stroke_time = 0.25`): carving leans the deck on its bushings and a pushing rider has no weight over the back truck to lean with, so steering authority drops to 15% at the start of a stroke and eases back as it finishes - measured at 340.9 deg of carve unpushed against 174.4 deg while pushing. Damps steering only, never the impulse, via a physics-side `_since_push` timer independent of any animation. New suite `tests/carve_and_push.tscn` (8 cases) covers carving, pushing, the previously untested kickturn branch, and the anchor axle in both stances; it also retains three **disproven** hypotheses as permanent cases (latched `_realigning`, kickturn position teleport, carve axis drift). Existing suites reproduce 24/24 with every figure identical.
- [x] **FootRig Rebuilt as a Spring-Driven Pose Solver (behaviour-preserving, increment 1 of `01_FOOT_ANIMATIONS.md`):** Replaced the four-method `FootRig` (`settle`/`hover`/`lower`/`animate`, all of which wrote both feet at once and only on the Y axis) with a **per-foot state machine over a critically-damped spring**. Each foot is a `Channel` carrying its own state, phase clock, rest transform (position **and** rotation) and spring velocity; each state solves a *target pose* displaced from rest, and the spring integrates the shoe toward it — so weight, overshoot and settling emerge rather than being authored, and an interrupted animation cannot snap. Foot posing collapsed from **three** points in the frame pipeline into one call at step 8c, with arbitration now readable top-to-bottom in `_advance_states()`. Push cadence gated: `start_push()` refuses while either foot is off its rest pose (presses are dropped, not queued), while the physical impulse stays unconditional so no animation gates a gameplay outcome. Severed the last presentation→physics coupling: `update_stance_facts()` now consumes the shoes' **rest offsets** instead of live node positions, retiring the hand-maintained "leading foot must never cross Z=0" invariant and freeing all future animations. Fixed nose/tail classification, which measured against `BoardPivot`'s −Z (the *rider's* forward) and so reported the wrong deck end after any landed shove-it; it now XORs the mounted end against `deck_reversed` read off `board_mesh`. Plumbed **stick flick speed** (`StickPoller.Sample.left_speed`/`right_speed` → `TrickSignature.flick_speed`), the measurement that separates a lazy flip from an explosive one — recorded but not yet consumed, and intended to drive both shoe arc speed and `flip_speed_deg` off one number. Verified: both suites reproduce **24/24 with every printed figure identical** to the pre-change baseline.
- [x] **Root-Cause Separation of Shove-it/Varial Scooping & Flatground Manual Initiation:** Audited and permanently solved accidental nose elevation when scooping circular thumbstick arcs for Pop Shove-its, Varial Flips, and Hardflips from 4-wheel riding. Identified the geometric root cause: sweeping along the controller's outer circular rim ($length \approx 1.0$) inherently drops Cartesian $y$ components ($1.0 \to 0.70 \to 0.50$), which previously triggered Cartesian-only manual checks ($y \le 0.90$). Re-architected 4-wheel manual initiation in `SkaterController._apply_grounded_board_pitch()` to enforce true polar constraints: initiating a new manual requires an interior polar magnitude ($0.20 < length \le 0.90$) and zero active scoop rotation ($max\_swept\_angle < 10.0^\circ$). Outer-rim circular compression and fast rotational arcs now remain 100% immune to nose lift while preserving seamless manual continuity once an active balance is established.
- [x] **Shoe Surface Contact, Ankle Peg Alignment & Camera Framing Calibration:** Calibrated shoe resting height in `SkaterRig.tscn` from `Y = 0.055m` down to `Y = 0.0155m` based on precise deck mesh bounding box measurements (`max_y_at_bolt = 0.0160m`, shoe sole `min_y = 0.0006m`), putting shoe soles in perfect physical contact with the griptape over the bolts when riding. Centered yellow ankle pegs inside the shoe collar openings by adjusting `PegPivot` from `X = -0.075m` to the measured rim center at `X = -0.050m`, and refined cylinder dimensions to `0.015m` radius and `0.12m` height for a cleaner visual gauge. Pulled the chase camera boom in tighter and at a slightly lower angle (`Y = 1.05m`, `Z = 1.80m`, `-21.5^\circ` pitch) for enhanced character framing without altering camera follow dynamics or regression tolerances.
- [x] **Physics-Driven Dynamic Pop Leveling, Upgraded Rotation Rates & Procedural Mid-Air Catch Stomp (via `/grill-me` alignment):** Rebuilt pop pitch kinematics to be truly dynamic and adaptable to future flick intensity mechanics. Initial pop pitch now scales proportionally with takeoff impulse (`_takeoff_vertical_velocity`), while airborne pitch leveling (`_apply_airborne_board_pitch`) dynamically smoothly tracks the ratio of live ascending velocity against takeoff velocity (`clamp(vertical_velocity / _takeoff_vertical_velocity, 0.0, 1.0)`). As gravity decelerates the ascent, the deck pitches steeply upward at takeoff ($50^\circ$ standard full pop) and smoothly levels out flat exactly at jump apex (`vertical_velocity == 0.0`), tracking the parabolic apex without static duration timers. Upgraded base rotation rates to $1020^\circ/\text{s}$ flip speed ($17^\circ/\text{frame}$) and $540^\circ/\text{s}$ spin speed ($9^\circ/\text{frame}$) so standard Kickflips naturally finish their rotation right around apex ($\approx 0.35\text{s}$). Built a new procedural `STOMPING` foot pose state in `FootRig.gd` (`execute_catch_stomp()`) and coupled it to rotation completion in `SkaterController.gd`: as soon as a flip completes revolution in mid-air (or immediately at apex for Ollies/Nollies), both feet drop simultaneously onto the griptape to catch the board mid-air before beginning the descent. Verified: 35/35 regression test cases pass across all three test suites (`curb_flip_repro.tscn`, `ground_physics.tscn`, `carve_and_push.tscn`).
- [x] **Non-Linear Pop Leveling Curve & Multi-Axis Rotational Complexity Coupling:** Solved sustained pop tilt and fixed universal catch timing across combined trick rotations. Replaced linear velocity-ratio pop leveling with an exponential power curve (`pow(ratio, pop_leveling_exponent)` with `pop_leveling_exponent = 2.5`), simulating explosive tail strike tilt at takeoff ($50^\circ$) that rapidly drops as the front foot levers the deck during early ascent, gliding horizontally through jump apex. Implemented physical multi-axis inertial coupling (`rotational_complexity_coupling = 0.15`) in `_impart_deck_rotation()`: simultaneously rotating in 3D across roll and yaw adds gyroscopic completion drag scaled by total sweep density. Simple tricks (Kickflips/Ollies/180s) catch cleanly at apex ($\approx 0.35\text{s}$, frame 21), Varial Flips catch at early descent ($\approx 0.43\text{s}$, frame 26), and 360 Flips (Tre Flips) catch later in descent ($\approx 0.45\text{s}$, frame 27), delivering an authentic physics-driven timing hierarchy. Verified: 35/35 regression test cases pass with exact numerical fidelity and multi-axis rotation synchronization.
- [x] **Fixed the Ice-Slide on Standing Directional Pops (`max_realign_angle_deg`):** Resolved a reported bug where popping with the foot held wide in the tail pocket (e.g. $-146^\circ$) from a standstill made the board glide sideways across the ground as though the surface were ice, and left the chase camera parked ~$90^\circ$ off. Root cause was in `_evaluate_touchdown_landing()`: `_landing_residual` was armed with the *entire* arrival speed whenever a landing was fully sideways. The budget's pacing rests on the small-angle relation $v_{lat} \approx \text{speed} \times \text{angle}$, which needs an along-axis component to rotate travel toward — at ~$90^\circ$ it rotates nothing and merely decays speed linearly over ~$1\text{s}$, while `wheel_side_grip = 40` never engages once because lateral never exceeds the budget (measured: **0 grip-engaged frames**). Arming is now bounded by `land_along * tan(max_realign_angle_deg)` with `max_realign_angle_deg = 45.0`. Measured on a stationary pop at $-146^\circ$: post-landing ground slide **0.583 m over 1.08 s → 0.009 m over 0.08 s**, with the airborne sideways displacement (the intended "land slightly left/right") completely unchanged at 1.12 m/s takeoff. Verified: 35/35 across all three suites, with every printed number byte-identical to baseline except `land 100 deg @ 1.5 m/s`, which was the same latent bug — baseline held 1.157 m/s at $-89.15^\circ$ a full 0.23 s after touchdown (travel turning at $3.7^\circ/\text{s}$ against an intended $60$); it now sheds to 0.32 m/s in two frames.
- [x] **Chase Camera No Longer Reads a Sideways Hop as a Heading:** Fixed the second half of the standing-directional-pop report — the camera swung ~$90^\circ$ during a stationary pocket pop and stayed parked there, filming the rider from the side after they had never turned. `ChaseCamera.follow()` gated `_travel_heading` on *total* flat speed, and a directional pop imparts up to $1.5\text{ m/s}$ of purely lateral velocity, well past `travel_min_speed` ($0.6$); once the rider landed and stopped, the guard froze that sideways heading permanently. The heading is now derived only when the component of velocity **along the rolling axis** clears the threshold: a heading is where the rider is *carried*, and sideways motion translates a rider whose heading is unchanged. Measured: stationary pop camera pan **$89.9^\circ \to 0.0^\circ$** across every tested pocket angle, while a pop at $7\text{ m/s}$ still steers the camera identically ($12.5^\circ / 9.3^\circ / 4.4^\circ$). Costs the bank reversal nothing by construction — gravity reverses travel *along* the axis, which is the component tested. Verified: 35/35 across all three suites, every figure identical except `ground_physics` "land 100 deg @ 1.5 m/s", whose max camera step **improved $2.42 \to 1.04$** with `offCentre` unchanged at $12.2$.
- [x] **Pop Levels Through Apex, and the Feet Land Instead of Being Sucked Home:** Two feel fixes to the pop-leveling and catch-stomp work. **(1) The pop stick is now consumed at takeoff (`TrickState.pop_load_spent`).** A rider pops by flicking the *opposite* stick, so the loading stick is still buried when the board leaves the ground — and airborne pitch read that as a live request, holding the deck at $+24^\circ$ nose-up for the whole flight (`+55 +46 +36 +29 +26 +25 +24 +24 +24`) and landing them in a manual they never asked for. Airborne pitch now reads `airborne_front_stick()` / `airborne_back_stick()`, which report a spent stick as centred until it returns to neutral; measured profile is now `+43 +32 +22 +13 +7 +3 +1 +1 +0` landing at **$+0.0^\circ$**, identical whether the stick is held or released. Grounded pitch and the touchdown manual catch are untouched. Also clamped the airborne pitch target to $\pm$`pop_pitch_deg`: the three terms *add*, so re-applying pitch mid-ascent stacked to $74^\circ$ — currently masked only by `airborne_pitch_follow` being too slow to reach it, i.e. a landmine under any future responsiveness tuning. **(2) The catch stomp got its own spring.** It was running on the riding pair (critically damped — by definition the fastest arrival *that does not overshoot*), so the feet drifted $0.16\text{m}$ over $0.25\text{s}$ and were still $0.008$/$0.020\text{m}$ short of the deck at touchdown, where `settle_now()` snapped them flat: the asymptote-then-teleport curve that reads as shoes being *sucked* onto the board. Added `stomp_stiffness = 1600` / `stomp_damping_ratio = 0.6` / `stomp_stagger`, giving a 6-frame ($0.10\text{s}$) arrival with $2.1\text{mm}$ of compression ring past the deck. Both feet now reach rest **before** touchdown ($+0.0000$/$+0.0000$), so `settle_now()` is a no-op and the landing no longer papers over an unfinished animation. Damping was tuned against the **discrete** integrator — semi-implicit Euler adds numerical damping, so $0.70$ and $0.65$ ring by exactly zero at this stiffness and tick. Verified: 35/35 across all three suites with every printed number byte-identical to the previous run.
- [x] **Airborne Foot Tuck Rebuilt as a Ballistic Parabola (removes the "levitate, then stomp" curve):** User feedback: the foot motion arc "has this levitating, then stomp feel — this kind of motion curve exists nowhere in nature", and the two feet felt "disjointed... one snaps, then the other, very robotic". Both were correct and both were architectural. `flip_hover_height` was a **fixed position target**, so the shoe sprang to $0.18\text{m}$ and then sat there **motionless for ~10 frames** ($0.17\text{s}$) before the target stepped discontinuously back to rest — a plateau at zero velocity, which nothing in free fall does. Replaced with a true constant-acceleration arc, `4p(1-p)`, parameterised on the new `SkaterController._flip_progress()` (measured off the rotation itself, so it needs no per-trick duration and survives the gyroscopic drag). The foot now leaves the deck with upward speed, peaks halfway through the rotation, and returns arriving at the speed it left with — so **the plant is in the arc** and the separate catch event is gone (`execute_catch_stomp()`, `stomp_delay`, `stomp_stagger` and the inert apex dual-stomp all deleted). The robotic stagger went with it: both feet are on one body and now leave and return together (measured $0.0000\text{m}$ apart at landing). Also deleted the apex catch-stomp path for straight ollies, which was measurably inert — feet sat at exactly $+0.000$ for all 38 airborne frames, because a deck that never spins never lifts the feet, so "stomping" had nothing to travel from. Verified: 35/35 across all three suites, every printed number byte-identical. **Known flaw:** minimum deck clearance measures $-0.0006\text{m}$ at f+2 — the spring lags the arc at the very start while the deck is already rotating, so the edge passes $0.6\text{mm}$ through the shoe. Sub-visible, but it is a real intersection.
- [x] **Stage 1 — `RiderBody`: the skater becomes a body of its own (provable no-op):** First stage of separating the rider from the board. Until now the physics simulated exactly **one** body — the deck — and the skater was a drawing hung off it, which is the shared root of two problems: the feet had no physical source of motion (so every airborne curve was an authored imitation needing a special case per situation), and `BoardPivot.rotation.y` carried the rider's body spin **and** the board's 0/180 switch-stance flip with no way to tell them apart, making a boardslide (deck 90° across travel, rider still facing near-forward) inexpressible. Added `scripts/player/RiderBody.gd` on a new `RiderTorso` node, a **sibling** of `BoardPivot` under `SurfaceAlign` — the feet deliberately stay in the deck's frame, since that is what makes switch/fakie mirroring inherited rather than compensated, and only the torso separates (which is also the anatomy). Aerial body spin now belongs to the rider and the board is *carried round* with them; `pivot_reversed()` reads the rider's frame, because being switch is a fact about the person. **The coupling is rigid on purpose** — the board takes the shoulders' delta in full, so this reproduces the previous behaviour exactly; the torsion spring that lets them diverge is introduced separately, where it can be felt and judged. Touchdown now parks both frames on the nearest half-turn (rounded independently, so the deck's own `lateral_pop_yaw_deg` cannot leak into the rider's frame) — without that the shoulders accumulate every spin ever taken. Verified: **35/35 with every printed number byte-identical.** `carve_and_push`'s "kickturn switch / after 180" caught the one real hazard en route (BUG_ARCHIVE #6's axle anchor) because it constructed switch stance by setting the board alone; a landed body-180 turns rider and board together, so the harness now sets both.
- [x] **Stage 3 — the feet become the end of the rider's legs (retires the authored tuck entirely):** The airborne foot motion is no longer a curve. `RiderBody` gains a leg length with a spring, an anatomical tuck limit (`leg_min`), and a contact clamp; `FootRig` composes `node.position = sprung_animation_pose + foot_lift`. **Key simplification:** the rider and the board are *both* projectiles, and gravity cancels exactly in the difference between two projectiles — so their separation is not governed by gravity at all, only by the legs. There is therefore no second ballistic integrator to subtract from the board's; it would be redundant and would drift. The one authored quantity is `tuck_impulse`, an **effort** scaled by `pop_impulse_scale`, never a path. **Deleted:** `_tuck_arc()`, `tuck_peak_height`, `_flip_progress()`, `Frame.flip_progress`. **What now falls out for free, measured:** a straight ollie tucks (peak lift $0.158\text{m}$, previously *exactly zero for all 38 airborne frames* — a hole dug twice, in two different mechanisms); effort scales with the pop ($0.158\text{m}$ at full load vs $0.016\text{m}$ on a gentle ledge drop); and the feet reach the deck under their own dynamics by touchdown ($+0.0000$ in every case), so `settle_now()` is no longer covering for an unfinished animation. Also fixed a double filter: springing the shoe toward its own leg length lagged the motion and cost $16\text{mm}$ of deck clearance in the first frames of a flip while the deck turned fastest — the leg is now applied rigidly *after* the spring, taking worst-case clearance to $-0.0003\text{m}$. **New suite `tests/foot_rig.tscn`** (clears CLEANUP #5): 12 cases across `{regular, goofy} × {forward, switch}`, asserting tuck-on-every-pop, effort scaling, feet home at touchdown, no NaN, no grounded lift, and no divergence between two feet that share one pair of legs. Verified by falsification — zeroing `tuck_impulse` and unscaling the effort each fail it. All four suites: 12/12, 12/12, 11/11, 12/12, with every pre-existing figure byte-identical.
- [x] **The Ollie Rule — feet only leave the deck when the deck needs the room:** User feedback: *"feet detach from the board on ollies. Any plain ollie based tricks (i.e. no spin or flip applied to the board) should always have the feet in contact with the board."* Correct, and it exposes a mis-modelling in Stage 3. In a real ollie the board is dragged up **by** the front foot, so tucking the knees raises the rider's **hips** while the feet stay planted and the deck rises with them — the feet release only to let the deck turn over underneath. Stage 3 had over-corrected from the opposite failure and routed the leg's tuck to the shoes unconditionally. The tuck now reaches the feet only while `deck_is_spinning`, so a plain ollie holds contact for the whole jump at any pop strength. Consequently `leg_stiffness` is re-sized against the **rotation** rather than airtime ($30 \to 80$, `tuck_impulse` $1.35 \to 2.15$): the tuck must outlast the deck turning *and* be spent by the time it stops, since that is the instant the feet take the deck back. Those two constraints fight near the end of a flip, which left $12\text{mm}$ of clipping at f+18 — so added `Frame.deck_reach`, the deck's own silhouette (`deck_half_width * |sin(roll)|`), as a **floor** beneath the feet. Taking the greater of leg-tuck and floor keeps it one visible motion: the leg is above the floor for most of the flip, and the floor only takes over at the end, carrying the feet to exactly zero as the deck arrives flat (so handing them back needs no blend). Intersection is now impossible **by construction** — worst-case clearance measured $-16\text{mm} \to -0.6\text{mm}$ (hand-authored parabola) $\to \mathbf{+3.8mm}$. The floor cannot disturb an ollie, since reach is zero at zero roll. `tests/foot_rig.tscn` now asserts the ollie rule at two pop strengths across all four stances. All four suites: 12/12, 12/12, 11/11, 12/12, every pre-existing figure byte-identical.
- [x] **Stage 2 — torsion between the rider and the board (rigid limit, provable no-op):** The deck no longer takes the shoulders' rotation outright; turning the shoulders winds a **twist**, and relaxing that twist is what turns the board. `twist_deg` is held as explicit state rather than derived from the two frames' yaws — load-bearing, because the deck kicking out under a directional pop (`lateral_pop_yaw_deg`) *is* a twist, and a coupling that re-derived it every frame would relax it the instant it was applied; that write now routes through `wind_twist()` instead of going behind the coupling's back. Comfortable range is **asymmetric** (`twist_external_deg = 45`, `twist_internal_deg = 25`) because hips externally rotate far further than internally — the anatomical fact that should make a frontside noseslide leave the body short of the board while a backside boardslide lets it come all the way round, and which yields the $20^\circ$–$35^\circ$ crooked/Smith offsets for free rather than from a per-trick table. Goofy mirrors it. `twist_follow` defaults effectively rigid (a plain relaxation rate, not a spring — a spring stiff enough to be rigid at $60\text{Hz}$ sits on the edge of instability), so **all four suites reproduce every figure byte-identical**. Verified by probe that the mechanism is real rather than dormant scaffolding: at `twist_follow = 12` the board lags the shoulders by $36^\circ$, at $5$ it saturates against the anatomical clamp, and the clamp mirrors correctly for goofy ($+45/-25$ regular, $+25/-45$ goofy). **One real bug caught in the process:** truncating twist past the limit silently discarded the excess, so the tracked twist and the board's actual offset walked apart — measured $41^\circ$ of twist against $171^\circ$ of real lag. A joint at its limit is rigid, so the excess now drags the board round bodily and the two agree exactly. Coverage deferred to the grind work per CLEANUP #8, on the same "write it alongside the first real use" principle that worked for `FootRig`.
- [x] **Flip tricks failing intermittently — the tail's compression is now physical state (resolves a reported bug):** User report: *"Every so often the pop then flick sequence fails due to timing issues… sometimes the input sequence feels like it should result in a completed trick, but the board never leaves the ground."* Two distinct causes, both wearing the same face, both from treating the stick as the state. **(1)** A rider pops by flicking the *opposite* stick, so both thumbs move at once and the loading one is usually already on its way home when the flick lands — yet `_release_pop()` read `load_stick.length()` **live** at that instant, making pop height a function of thumb synchronisation rather than of how hard the rider loaded. Measured: stick at `1.00 / 0.70 / 0.40 / 0.10 / 0.00` on the flick frame gave `0.80 / 0.80 / 0.19 / 0.01 / 0.00` metres of jump — roughly **two frames of timing between a full ollie and nothing**, with the trick still registering (deck flipped, name resolved) but carrying no height. **(2)** On a slower flick throw the loading thumb crossed the deadzone *before* the flicking one reached it, putting both sticks under `manual_zone_min` on the same frame, so the reset clause dropped the load mid-gesture and no pop happened at all. Fixed together by making the tail's compression (`TrickState._load`) physical state that follows the thumb up instantly and springs back at `load_release_rate = 2.5` units/s — the tail is already compressed when the flick happens, and letting go of the stick does not uncompress it instantly. Direction stays live, so gliding the foot across the tail's pockets still aims the pop in real time; only the magnitude is remembered. The reset now also requires the compression to have bled off. **Forgiveness is real but bounded, and does not swallow the graded response it sits on:** full load with the thumb fully home at the flick → full $0.802\text{m}$ pop; steady gentle hold $0.45$ → $0.020\text{m}$; light hold $0.25$ → un-popped ledge drop; load hard, ease off, wait 30 frames → $0.002\text{m}$, i.e. it springs back rather than latching. **New suite `tests/pop_gesture.tscn`** — 10 cases across regular/goofy, driven through the real input chain, because both bugs were invisible to all four existing suites (`curb_flip_repro` injects `pop_impulse_triggered` and never runs `_release_pop()`; the others never pop from a gesture). Verified by falsification: reverting to the live stick read fails it with exactly the reported symptom. All five suites pass with every pre-existing figure byte-identical.
